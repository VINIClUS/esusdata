# Discovery — e-SUS PEC 5.5.28, produção municipal (2026-09-24)

Evidence for adding PEC **5.5.28** to `pec_versions` of the `individual_encounter_modality` entry in
`contracts/compatibility/pec-adapters.json` (schema v2). Read-only throughout: every query ran as
`esus_leitura` with `default_transaction_read_only=on`; **nothing in the PEC or on the host was
modified**. No patient data is recorded here — only counts, catalog metadata and hashes.

## Access path

```
workstation ── LAN ── ssh -N -L 15434:127.0.0.1:5433 <admin>@<host>   (Windows 11 pt-BR, OpenSSH)
```

PostgreSQL listens on the host's loopback only (`5433` is closed from the LAN; `22` is open). The
read-only credentials come from the PEC installer's own `config\credenciais.txt` on the host and
live only in a local 0600 secret file outside the repository.

## Installation identity

| Fact | Value | Evidence |
|---|---|---|
| PEC version | **5.5.28** | newest `db/v<version>/` changeset in `tb_migracao` (PEC's Liquibase log): `5.5.28`, executed 2026-09-14 20:57, after `5.5.26`/`5.5.25` in the same upgrade |
| PostgreSQL | **9.6.13**, Visual C++ build 1800, 64-bit | `select version()` |
| Database | `esus` | connection |
| Municipality | 3541307 — the only `co_ibge` in `tb_fat_atendimento_individual` | `count(distinct m.co_ibge)` = 1 |
| Read model / role | `PEC_DW` / `PRONTUARIO` | same DW tables as CT 133 |

## ENG-43 fingerprints (7/7 byte-identical)

Measured twice against the same database: over JDBC by `JdbcCompatibilityCatalog`, and from the Rust
execution plane's own probe (`ExecPlaneAcquisition` computes the fingerprint from what the child
measured and logs it on a match). Both equal the values pinned when 5.4.37 was validated on CT 133.

| Object | Matrix (`signature_fingerprint`) | JDBC | Rust probe |
|---|---|---|---|
| `tb_fat_atendimento_individual` | `sha256:dd3436b5…3e67b1` | = | = |
| `tb_dim_tempo` | `sha256:79d80347…870b1b` | = | = |
| `tb_dim_municipio` | `sha256:832ac215…81280d` | = | = |
| `tb_dim_tipo_atendimento` | `sha256:34db0f5c…807491` | = | = |
| `tb_dim_unidade_saude` | `sha256:1a714cea…d83e7b` | = | = |
| `tb_dim_equipe` | `sha256:13fc0af0…a6906f` | = | = |
| `tb_dim_cbo` | `sha256:5569646b…a3402e` | = | = |

"=" means the full 64-hex digest matched the matrix, not just the prefix; the complete values are in
the matrix itself. The query checksum (`sha256:d3056dab…b90246`) is unchanged — the SQL did not change.

Order of the proof: the comparison ran first **with the matrix untouched** (5.4.37 declared,
matching the only entry then), so the gate did not depend on the relaxation it justifies. Only
after 7/7 matched was 5.5.28 listed, the binary rebuilt (it embeds the matrix), and the test re-run
with the true identity.

## `LEAF_SEMANTICS=2,3,5,6,7` coverage

`tb_dim_tipo_atendimento` has the same 11 rows and parent links as CT 133 (ids 1 and 4 are the
programado/espontâneo parents; 8–11 are other leaves or "Não informado").

| `co_dim_tipo_atendimento` | rows in `tb_fat_atendimento_individual` |
|---|---|
| 2 | 20 839 |
| 3 | 217 715 |
| 5 | 2 404 |
| 6 | 85 417 |
| 7 | 252 |
| **total** | **326 627** (2023-07-03 to 2026-08-31) |

Ids 8, 9, 10 and 11 never occur — the five-id mapping covers 100% of the history here too, and those
four stay `NOT_TESTED`.

## Live execution-plane run (true identity)

```
mvn -f apps/agent test -Dsurefire.reuseForks=false -Dtest=ExecPlaneLivePecTest \
  -Dobservatorio.execution-plane.live-pec=true \
  -Dobservatorio.execution-plane.live-pec.env-file=$HOME/.config/observatorio-aps/pec-253.env \
  -Dobservatorio.execution-plane.binary=$PWD/apps/execplane/target/release/observatorio-execplane
```

The secret file declares `PEC_VERSION=5.5.28` — the version the installation runs, not one
borrowed to reach a matrix entry. Result: **Tests run: 4, Failures: 0, Errors: 0, Skipped: 0**.

| Test | Outcome |
|---|---|
| `fingerprintsOfTheRealPecMatchTheMatrixEntryForItsDeclaredVersion` | 7/7 JDBC fingerprints = matrix (table above) |
| `acquiresOneMonthEndToEndThroughTheRustChild` | competência 2026-03: 10 029 rows, 0 exclusions, extract published, no uncertain outcome |
| `cancellingAfterRowsWereEmittedStopsTheQueryOnTheRealPec` | whole history; cancel at the first `progress`, `JobCancelledException` after 5.6 s, no extract, 0 active `observatorio-aps` queries afterwards |
| `wrongPasswordIsAnAuthenticationFailureWithoutCooldown` | `SOURCE_AUTHENTICATION_FAILED`, no uncertain outcome (one failed login in the server log) |

The 2026-03 total (10 029) equals CT 133's psql baseline for the same municipality and competência
(`docs/discovery/2026-09-19-pec-ct133.md`), consistent with both installations holding the same
municipality's records up to April 2026.

Production load: each live test ran once in this final pass (the fingerprint and one-month tests
also once in the pre-relaxation gate); nothing ran in a loop.

## Source diagnostic: execution plane vs pgJDBC (ADR 0017 gate)

Same command as above, same secret file, after `ExecPlaneLivePecTest` gained the two diagnostic
cases. Each one runs the pre-ADR-0017 pgJDBC diagnostic (`JdbcSourceDiagnostics`, kept in
`src/test`) and the execution-plane diagnostic (`SourceDiagnosticsService` +
`ExecPlaneConnectivityCheck`) against the same source and requires the **whole** `Diagnostics`
record (outcome, detail, budget) to be equal. Result: **Tests run: 6, Failures: 0, Errors: 0,
Skipped: 0** (the four acquisition cases above re-ran green as a regression).

| Test | Outcome on both paths |
|---|---|
| `diagnosticMatchesTheJdbcReference` | `CONNECTED`, detail `null` |
| `wrongPasswordDiagnosticMatchesTheJdbcReference` | `SOURCE_AUTHENTICATION_FAILED`, detail `source authentication failed` (one failed login per path in the server log) |

`SOURCE_PERMISSION_DENIED`, `CONNECTION_FAILED`, `SOURCE_BUSY`, `DESTINATION_NOT_ALLOWED` and the
unknown-installation-role refusal are not provoked in production (that would mean creating roles on
a read-only installation); `SourceDiagnosticsDifferentialLiveTest` covers them against
`postgres:9.6.13`, 7/7 equal.

Production load for this gate: one successful and one failed login per path, plus the four
acquisition cases once. The tunnel was opened for the run and closed right after.

After pgJDBC left `main` (ADR 0017), the same command re-ran only
`fingerprintsOfTheRealPecMatchTheMatrixEntryForItsDeclaredVersion`,
`acquiresOneMonthEndToEndThroughTheRustChild` and the two diagnostic cases: **Tests run: 4,
Failures: 0, Errors: 0, Skipped: 0**. The `-Pweb` jar carries no `postgresql` artifact.
